const numero1=20;
const numero2="20";

console.log( parseInt(numero2) ); //parseInt es una función , nombre función seguido de paréntesis, es función
console.log(numero1.toString()); //toString es un método . seguido de la función es método
